#include <stdio.h>
int main() {
    int n, rem, base = 1, dec = 0;

    printf("Enter binary: ");
    scanf("%d", &n);

    for(; n != 0; n /= 10) {
        rem = n % 10;
        dec += rem * base;
        base *= 2;
    }

    printf("Decimal = %d", dec);
    return 0;
}
