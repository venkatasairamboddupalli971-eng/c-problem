#include <stdio.h>
int main() {
    int n, rem, bin = 0, place = 1;

    printf("Enter decimal: ");
    scanf("%d", &n);

    for(; n != 0; n /= 2) {
        rem = n % 2;
        bin += rem * place;
        place *= 10;
    }

    printf("Binary = %d", bin);
    return 0;
}
