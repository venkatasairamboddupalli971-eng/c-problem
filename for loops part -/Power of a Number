#include <stdio.h>
int main() {
    int a, b, i, result = 1;

    printf("Enter base and power: ");
    scanf("%d %d", &a, &b);

    for(i = 1; i <= b; i++) {
        result *= a;
    }

    printf("Result = %d", result);
    return 0;
}
