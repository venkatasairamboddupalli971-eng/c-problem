#include <stdio.h>
int main() {
    int a,b,result=1,i;

    scanf("%d%d",&a,&b);

    switch(b==0) {
        case 1: printf("1"); break;
        case 0:
            for(i=1;i<=b;i++)
                result*=a;
            printf("%d",result);
    }
}
