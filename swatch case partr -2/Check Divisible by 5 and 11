#include <stdio.h>
int main() {
    int num;
    scanf("%d",&num);

    switch(num%5==0 && num%11==0) {
        case 1: printf("Divisible by both"); break;
        case 0: printf("Not divisible");
    }
}
