#include <stdio.h>
int main() {
    int num,i,fact=1;
    scanf("%d",&num);

    switch(num<0) {
        case 1: printf("Invalid"); break;
        case 0:
            for(i=1;i<=num;i++)
                fact*=i;
            printf("Factorial = %d",fact);
    }
}
