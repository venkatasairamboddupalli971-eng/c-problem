#include <stdio.h>
int main() {
    int num,count=0;
    scanf("%d",&num);

    switch(1) {
        case 1:
            while(num>0) {
                count++;
                num/=10;
            }
            printf("Digits = %d",count);
    }
}
