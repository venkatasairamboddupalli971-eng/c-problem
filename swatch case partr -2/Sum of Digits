#include <stdio.h>
int main() {
    int num,sum=0;
    scanf("%d",&num);

    switch(1) {
        case 1:
            while(num>0) {
                sum += num%10;
                num/=10;
            }
            printf("Sum = %d",sum);
    }
}
