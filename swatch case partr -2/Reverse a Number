#include <stdio.h>
int main() {
    int num,rev=0;
    scanf("%d",&num);

    switch(1) {
        case 1:
            while(num>0) {
                rev = rev*10 + num%10;
                num/=10;
            }
            printf("Reverse = %d",rev);
    }
}
