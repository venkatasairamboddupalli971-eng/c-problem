#include <stdio.h>
int main() {
    int a,b,temp;
    scanf("%d%d",&a,&b);

    switch(1) {
        case 1:
            temp=a;
            a=b;
            b=temp;
            printf("After swap: %d %d",a,b);
    }
}
