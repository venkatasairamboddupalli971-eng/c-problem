#include <stdio.h>

int main()
{
    char str[50];
    int i;

    scanf("%s", str);   // read string

    for(i = 0; i < 4; i++)
    {
        printf("%s\n", str);   // print string 4 times
    }

    return 0;
}
