#include <stdio.h>

int main() {
    int a[20], n, i;
    int sum = 0;
    float avg;

    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        sum = sum + a[i];
    }

    avg = sum / (float)n;

    printf("%.0f", avg);
    return 0;
}
