#include <stdio.h>

int main() {
    int n, i = 2, sum = 0, count = 0;

    scanf("%d", &n);

    while(count < n) {
        sum = sum + i;
        i = i + 2;
        count++;
    }

    printf("%d", sum);
    return 0;
}
