#include <stdio.h>
int main() {
     int salary;
     scanf("%d",&salary);
     if (salary>30000)
     {
        printf("bouns eligible");
     }
     else if(salary<30000){
        printf("not eligible");
     }
     else {
      printf(" bouns not eligible");
     }
     return 0;
     }
