#include <stdio.h>
int main() {
     int a;
     scanf("%d",&a);
    if(a>80)
     {
        printf(" Speed Limit Exceeded");
     }
     else 
     {
        printf("within Speed Limit");
     }
     return 0;
}
 
