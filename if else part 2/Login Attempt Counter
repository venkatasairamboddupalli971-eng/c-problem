#include <stdio.h>
int main() {
     int a;
     scanf("%d",&a);
    if(a>3)
     {
      printf("Account Locked  ");
     }
     else 
     {
        printf(" Account ALLOWED");
     }
     return 0;
}
 
