#include <stdio.h>
int main() {
     int a;
     scanf("%d",&a);
    if(a<1000)
     {
      printf("low blance");
     }
     else  
     {
        printf("Sufficient Balanc");
     }
     return 0;
}
 
