#include <stdio.h>
int main() {
    int m1, m2, m3, total;
    scanf("%d %d %d", &m1, &m2, &m3);

    total = m1 + m2 + m3;

    if(m1 >= 35 && m2 >= 35 && m3 >= 35) {
        printf("Pass\n");
        printf("Total %d", total);
    } else {
        printf("Fail");
    }

    return 0;
}
