#include <stdio.h>
int main() {
    int type, bill;
    scanf("%d %d", &type, &bill);

    switch(type) {
        case 1: bill -= bill * 0.05; break;
        case 2: bill -= bill * 0.15; break;
    }

    printf("Pay ₹%d", bill);
    return 0;
}
