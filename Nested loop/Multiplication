#include <stdio.h>
int main() {
    int n = 2;

    switch(n) {
        case 2:
            for(int i=1; i<=5; i++)
                printf("%d x %d = %d\n", n, i, n*i);
            break;
        default:
            printf("Other number");
    }
}
