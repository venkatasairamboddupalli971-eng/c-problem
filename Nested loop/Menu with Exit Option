#include <stdio.h>
int main() {
    int choice = 3;

    switch(choice) {
        case 1: printf("Start"); break;
        case 2: printf("Settings"); break;
        case 3: printf("Exit"); break;
        default: printf("Invalid");
    }
}
