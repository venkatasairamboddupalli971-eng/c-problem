#include <stdio.h>

int main() {
 int a,b,i,gcd;
 char result[45];
 scanf("%d%d",&a,&b);
 for(i=1;i<=a&&i<=b;i++){
 if(a%i==0&&b%i==0)
 gcd=1;
}
sprintf(result,"%d",gcd);
printf("%s",result);
return 0;
}