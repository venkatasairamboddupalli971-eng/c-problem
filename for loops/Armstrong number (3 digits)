int num = 153, temp = num, sum = 0;

for(; temp != 0; temp /= 10) {
    int digit = temp % 10;
    sum += digit * digit * digit;
}

if(sum == num)
    printf("Armstrong");
else
    printf("Not Armstrong");
