#include <stdio.h>
int main() {
    int i, j, n, fact, sum = 0;

    printf("Enter n: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        fact = 1;
        for(j = 1; j <= i; j++)
            fact *= j;

        sum += fact;
    }

    printf("Sum = %d", sum);
    return 0;
}
