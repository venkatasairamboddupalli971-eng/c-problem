#include <stdio.h>
int main() {
    int A, B, lcm;
    scanf("%d %d", &A, &B);
    for(int i = 1; ; i++) {
        if(i % A == 0 && i % B == 0) {
            lcm = i;
            break;
        }
    }
    printf("%d", lcm);
    return 0;
}
 
