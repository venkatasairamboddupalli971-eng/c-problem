#include <stdio.h>
int main() {
    int N, prime = 1;
    scanf("%d", &N);
    for(int i = 2; i * i <= N; i++)
        if(N % i == 0)
            prime = 0;
    if(prime) printf("Prime");
    else printf("Not Prime");
    return 0;
}
