class Solution {
    public static void depth(TreeNode cur, List<Integer> store){
        if(cur==null) return;
        store.add(cur.val);
        depth(cur.left, store);
        depth(cur.right, store);
    }
    public List<Integer> preorderTraversal(TreeNode root){
    List<Integer> items = new ArrayList<>();
    depth(root,items);
    return items;
    
        
    }
}
